apiVersion: v1
kind: Service
metadata:
  name: your-app-service
  namespace: your-app
  labels:
    app: your-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: your-app